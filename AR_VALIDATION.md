# AR 第一批功能验证记录

日期：2026-09-23。环境：Windows，Python 3.12，OpenCV 4.12.0，NumPy 2.2.6，Pillow 11.3.0，python-frontmatter 1.1.0，PyYAML 6.0.3。

## 已执行

- 14 项自动测试全部通过，包括焦点选择与消失、循环选择、显隐和暂停、字体范围、紧凑/展开状态、布局边界和避让、无显示运行、异常释放设备、原库登记和样本重载。
- 布局覆盖 320×240、640×480、1280×720、1920×1080 以及 16、22、40 像素字体；断言已放置的卡片在安全区内、不与检测到的人脸框或其他卡片相交。
- OpenCV Zoo 的 YuNet 和 SFace 实际加载成功，两个文件均核对 Git LFS SHA-256。
- 使用 OpenCV 官方测试图片执行真实模型链路：检测→陌生人→原服务登记→索引重载→识别→人物档案→HUD→重新建立管线后识别，全部通过。同一图片重识别余弦相似度为 1.0000；这不代表真实场景准确率为 100%。
- 原 `register_unknown_person` 终端登记流程使用自动输入完成新人物登记与同名追加样本，文件可由原数据层读回。
- 合成演示真实 GUI 窗口连续运行 45 帧后正常关闭；中文 HUD 截图已检查。
- 默认摄像头实际读取并处理 3 帧后释放，未保存摄像头图片、视频或人物样本。
- 使用官方样例生成的 12 帧测试视频验证完整入口与播放结束退出。测试素材与人物数据留在独立测试目录，不随代码包分发。
- 依赖一致性检查 `pip check` 通过。

## 验证边界

当前验证证明新增入口能够与本地原库联动，并能够读取本机摄像头。尚未进行长时间运行、多种摄像头、多人交叉、不同光照/角度的人脸准确率测试，也没有连接真实 AR 眼镜 SDK。

人物身份仍采用原项目的阈值、跟踪与历史投票逻辑。这批代码没有修改这些旧实现。登记期间使用原终端交互，因此窗口暂不处理按键；登记结束自动恢复。空间不足时省略卡片，长文本截断，避免遮挡检测到的人脸。

## 模型指纹

```text
face_detection_yunet_2023mar.onnx
8f2383e4dd3cfbb4553ea8718107fc0423210dc964f9f4280604804ed2552fa4

face_recognition_sface_2021dec.onnx
0ba9fbfa01b5270c96627c4ef784da859931e02f04419c829e83484087c34e79
```

测试命令与操作方式见 `AR_VIEW_GUIDE.md`。
